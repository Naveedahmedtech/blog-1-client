import { Typography } from '@mui/material';

const EmptyPostsMessage = ({ message }: any) => (
    <Typography variant="h6" align="center">{message}</Typography>
);

export default EmptyPostsMessage;
